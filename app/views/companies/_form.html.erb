<%= simple_form_for @company do |f| %>
  <%= f.input :name, placeholder: "Nome da empresa" %>
  <%= f.input :location %>
  <%= f.input :mail, as: :email %>
  <%= f.input :phone, as: :tel %>
  <%= f.button :submit, value: submit_text %>
<% end %>
